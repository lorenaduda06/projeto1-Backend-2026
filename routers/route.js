const express = require("express");
const db = require("../config/db");

// Requisição dos arquivos da pasta controller

// Método 'sync' com { force: true }

// Logar aluno

// Rotas (get, post, put e delete) + Controller

module.exports = route;